# 项目执行约定

在本仓库执行任何开发、修复、测试、清理数据或验收任务前，必须先读取根目录下的 `记忆.md`。

执行任务时应遵守 `记忆.md` 中的约定，尤其是：

- 测试完成后，只清理本次创建且能通过唯一标记明确识别的测试数据。
- 不使用会清空历史数据的全量重置方式，例如 `/api/test/reset`，除非用户明确要求。
- 开发或修改 bug 后，默认只进行简单冒烟测试。
- 除非用户明确要求执行全面测试，否则不主动执行完整回归、全量 E2E 或耗时较长的全面测试。

如果用户的当前明确指令与 `记忆.md` 冲突，以用户当前明确指令为准。
