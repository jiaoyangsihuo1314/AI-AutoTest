# 记忆：测试数据清理约定

## 核心原则

后续执行测试时，测试完成后不要删除之前已有的数据，只删除本次测试过程中由我创建、且能够通过唯一标记明确识别的测试数据。

如果无法确认某条数据是否属于本次测试，默认保留，不删除。

## 执行约定

- 不使用会清空历史数据的全量重置方式，例如 `/api/test/reset`，除非用户明确要求。
- 开发或修改 bug 后，默认只进行简单的冒烟测试，确认核心流程可用即可。
- 除非用户明确要求执行全面测试，否则不主动执行完整回归、全量 E2E 或耗时较长的全面测试。
- 创建测试数据时必须添加唯一标记，优先使用时间戳或本次任务前缀。
- 推荐标记格式：
  - `CODEX_TEST_YYYYMMDD_HHMMSS`
  - `TC-CODEX-*`
  - `E2E-CODEX-*`
- 测试结束后，只清理匹配本次唯一标记的数据。
- 可清理范围仅限本次创建的用例、套件、项目、功能、配置或临时记录。
- 对历史数据、非本次创建数据、无法确认归属的数据，一律保留。

## 清理前检查

清理任何测试数据前，先确认：

- 数据名称、ID、描述或备注中包含本次唯一标记。
- 数据确实由本次测试流程创建。
- 删除操作不会影响已有历史数据、用户数据或其他测试任务。
- 如果需要删除历史数据或执行全量重置，必须先获得用户明确授权。

## 验收标准

- 测试完成后，之前已有的数据仍然存在。
- 只有本次测试创建并带有唯一标记的数据被删除。
- 未带唯一标记或归属不明的数据没有被删除。
- 未经明确授权，不调用 `/api/test/reset` 等全量重置接口。
